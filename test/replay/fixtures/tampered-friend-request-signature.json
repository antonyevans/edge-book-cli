{
  "schema": "edge-book-replay-fixture/0.1",
  "title": "failure case: envelope tampered in transit fails signature verification",
  "description": "A friend_request whose payload was mutated after signing must be rejected by verifyEnvelope and record envelope.signature_failed — and must NOT create a contact or a pending approval.",
  "identities": {
    "mallory": {
      "seed": "3333333333333333333333333333333333333333333333333333333333333333",
      "handle": "mallory.replay.local",
      "display_name": "Mallory (synthetic)"
    }
  },
  "steps": [
    {
      "deliver": {
        "from": "mallory",
        "type": "friend_request",
        "message_id": "msg-replay-sig-001",
        "trace_id": "trace-replay-sig-001",
        "created_at": "2026-06-01T00:00:00.000Z",
        "expires_at": "2099-01-01T00:00:00.000Z",
        "body": { "note": "synthetic reproduction: body mutated in transit" },
        "tamper": "signature"
      },
      "expect": {
        "outcome": "signature_failed",
        "error": "invalid_signature",
        "events": [
          { "kind": "envelope.signature_failed", "fields": { "envelope_kind": "friend_request", "dedup_key": "msg-replay-sig-001" } },
          { "kind": "envelope.received", "fields": { "applied": false } }
        ]
      }
    }
  ]
}
